name: sreerag_dev
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  url_launcher: ^6.3.2
  font_awesome_flutter: ^11.0.0
  shimmer: ^3.0.0
  http: ^1.3.0
  firebase_core: ^4.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profile/
    - assets/projects/
    - assets/projects/lab_track/
